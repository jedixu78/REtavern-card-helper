# 交接清单 — 新会话从这里继续

> 写于 2026-07-27。上一会话因上下文耗尽结束。本文档自包含：不需要读上一会话的对话记录即可执行。
> 总规划见 [功能评估与开发规划-2026-07.md](功能评估与开发规划-2026-07.md)；已完成的工作看 `git log main` 与各提交说明（提交说明写得很详细，是最好的上下文来源）。

## 当前状态快照

- **分支**：`feat/s3-followups`（基于 main，领先 1 个提交 `71b297c`）。main 已含 P0/S1/S2 全部成果。
- **工作区**：干净。typecheck ✅ / **553 测试全过** ✅ / 0 lint 错误（仅 1 个刻意保留的 SettingsPage 警告）/ 构建 ✅。
- **未推送**：整个 main 都没推过 origin，推送由用户决定，不要主动推。
- 上一会话有两个后台任务（试聊 MVU 模拟器、工坊失败段补跑）随进程终止，**没有任何改动落盘**——下面的任务 1/2 从零开始做即可。

## 工作方法约定（保持同一质量标准）

1. **每个逻辑提交独立可编译**：提交后用 `git rev-list --reverse main..HEAD` 逐个 checkout 跑 `npx tsc --noEmit` 验证（历次都这么做的）。
2. **每批改动落盘前跑对抗式复查**：用 Workflow 起 2-3 个视角的审查 agent，每条发现再起独立 agent 试图驳倒，只修被确认的。历史三轮复查分别确认了 11/15/10 条真问题（含执行级复现），这一步不能省。
3. **并行 agent 严禁改 `src/i18n/translations.ts`**（并发读改写会互相丢编辑）：让它们报「键名+中文文案」清单，由主会话统一添加。注意：缺键时 `t()` 返回**空串**（不是键名），别信相反的说法。
4. 提交说明用中文、讲清楚「为什么」与复查修了什么；`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
5. TS strict 是开着的（TS6 默认，CLAUDE.md 有说明）；不要用 `any` 绕。

## 待执行任务（按序）

### 任务 1：试聊 MVU 变量模拟器（原任务 #30）

**问题**：试聊已能沙盒渲染状态栏（`src/services/chat-render.ts`），但 `{{format_message_variable::stat_data.xxx}}` 宏无变量引擎求值，原样显示——状态栏卡能验证布局、验证不了数值绑定。上一轮刻意没伪造假数据（显示假值比显示宏更误导）。

**先做逆向调研（不要跳过）**：读仓库根目录被 gitignore 的本地参考 `magvarupdate/`（MagVarUpdate 源码）与 `参考/` 下成品卡，搞清：① AI 回复中 `<UpdateVariable>` 块内 `_.set('路径', 旧, 新);//原因` 的真实语法（可能还有别的命令）；② 初始变量来源（`[InitVar]` 条目 YAML 格式见 `card-exporter.ts` 的 `buildInitvarYaml`，及开场白 EJS setvar 前缀）；③ 宏的消费侧语义。

**实现**：新建 `src/services/mvu-sim.ts`（纯函数+完整单测）：`parseInitialVariables(card)` / `applyUpdateBlocks(statData, aiReply)`（不可变更新+变更清单 {path,from,to,reason}）/ `substituteVariableMacros(html, statData)`（路径不存在保留原样并返回未解析清单）。接入 `useAIChat.ts`/`ChatPage.tsx`：会话开始解析初始值；每轮回复演进 statData；**重 roll 要回滚**（按消息序列重放最稳）；渲染前替换宏；检查器旁加「变量状态」折叠面板（stat_data 树+本轮变更+未解析宏）；`<UpdateVariable>` 块对用户隐藏（确认 chat-render 的脚本应用是否已覆盖）。与真实运行时的每个差距在代码注释与 UI note 里如实列出。YAML 手写够用子集，不引依赖。

### 任务 2：工坊失败段一键补跑（原任务 #26）

**问题**：工坊分段提取失败段只即时重试一次即永久跳过，章节内容静默缺失。failedChunks 已有跟踪与计数展示：跟踪在 `src/components/novel-workshop/NovelWorkshop.tsx`（约 :163/:229/:262），展示在 `panels/PipelinePanel.tsx`（约 :84），合并是 `novel-workshop-service.ts` 的 `mergePackagesLocally`（约 :370）。行号可能漂移，以 grep 为准。

**实现**：跑完后 failedChunks 非空 → PipelinePanel 显示「重试失败段(N)」；只重跑失败段，**复用现有单段提取函数**与并发/重试参数；成功并入（沿用 first-wins 合并语义，不要重新设计）；再失败留在列表可再点；checkpoint 里存取 failedChunks（恢复会话后按钮可用）；重试中禁再点/禁开始生成。该模块文案是硬编码中文还是 t() 先看现状、跟随现状。

### 任务 3：`data.description`/`personality` 归一化丢失（原任务 #28）

**问题**（S2 的 agent 主动上报，比 mes_example 更大的保真缺口）：第三方卡 description 在 `cardToDraft` 进 `characters[0].description`，靠向导 `syncCharacterEntries` 转世界书条目；但 `LibraryPage` 的 `cardToDraft→assembleCard` 直接归一化路径会彻底丢掉它。`personality` 则完全被丢弃。不能走直通层修（description 是本工具生成的已知字段，`stripOwnKeys` 纵深防御会剔掉它——这个防御不能弱化）。

**已定设计**（上一会话想清楚了）：
- `assembleCard` 的 `description`：对每个「有描述但没有任何已同步世界书条目」的角色（判定 = entryIds 无一命中现存条目 **且** 无任一条目内容等于/包含其描述——与 `token-budget.ts` 里 `contentMatchesDescription` 同一启发式），把这些描述 join('\n\n') 写入 `data.description`；否则维持 `''`（worldbook-first 架构不变）。向导正常流程里角色总是已同步（跨步骤 3 时注入），所以该条件几乎只对导入卡生效，不会造成向导卡的双份重复。
- `personality`：仿照 `mes_example` 做成 `WizardDraft.personality?: string` 往返直通（`cardToDraft` 读、`assembleCard` 写、`createEmptyDraft` 补 `''`），暂不做 UI（可后续挂在 Step 7 高级区）。
- **不 bump WIZARD_DRAFT_VERSION**（理由见提交 `0d6a3a1` 的说明：migrate 只认 {4,5,6}，bump 会清空存量草稿）。
- 补往返测试：第三方卡（有 description/personality、无匹配条目）导入→导出逐字段保真；向导卡（已同步角色）导出 description 仍为 `''`。

### 任务 4：世界书元数据保真 + 条目硬编码字段（原任务 #29）

**问题**：① `character_book.name` 被强制改写为 `${cardName}的世界书`、`description` 恒 `''`（第三方书名丢失）；② 一批无 UI 入口的硬编码条目 extensions 字段覆盖导入值：`match_persona_description` / `match_character_description` / `match_character_personality` / `match_character_depth_prompt` / `match_scenario` / `match_creator_notes` / `triggers` / `outlet_name` / `use_group_scoring`。

**已定设计**：
- ② 简单：把这批键从 `card-exporter.ts` 的 `OWN_ENTRY_EXT_KEYS` 移入 `ENTRY_EXT_IMPORT_WINS_DEFAULTS`（默认值抄 `buildSTExtensions` 现在写死的常量），加往返测试。这与 `useProbability`/`scan_depth` 在 S2 已做的处理完全同型（见提交 `0d6a3a1`）。
- ① 谨慎：加 `WizardDraft.bookName?: string` 与 `bookDescription?: string`；`cardToDraft` 读原书名/描述；`assembleCard` 用 `draft.bookName?.trim() || \`${cardName}的世界书\``，**`extensions.world` 必须与 `character_book.name` 保持相等**（有既有测试锁这个不变式，两处一起改）。⚠️ 关联风险：分阶段调度条目的 `getWorldInfo("书名",...)` 与 `WizardPage.tsx` 里 `escapeEjsDoubleQuoted(resolvedCardName...)` 的书名推导——书名可自定义后必须让调度条目的书名跟随同一来源，否则复活「阶段不切换」根因。grep `getWorldInfo` 与 `的世界书` 把所有书名推导点找全再动手。

### 任务 5：暴露 DIY/AI 自选阶段轴（路线图 S1-4 残留，从未建卡的疏漏）

评估发现 DIY/阶段轴自选模式已实现但 UI 未暴露（阶段轴被 7 个模板硬编码占满）。在 `StepStagedMode`/模板选择流程里找到已实现的 DIY 管线（评估提到「白名单门控、applyStagedTemplate、DIY 生成管线均无调用者」），接上入口。**先核实现状**——若实现残缺，如实降级为「手动增删阶段/自定义轴路径」的最小可用编辑并报告。

### 任务 6：检修（用户明确要求的收尾）

1. 全部任务落盘后：对整个 `main..HEAD` diff 跑一轮**对抗式复查 Workflow**（视角建议：MVU 模拟器正确性 vs magvarupdate 真实语义、导出往返保真、工坊状态机与 checkpoint 兼容），修完所有确认项。
2. 分组提交 + 逐提交独立编译 sweep + 全量 typecheck/test/lint/build。
3. 更新 `docs/功能评估与开发规划-2026-07.md`：把 P0/S1/S2 及本批已完成项标记为完成（附提交号），余下项（S3 生态差异化：V2/V3/charx 三输出、Macros 2.0 校验、ST 直连、群聊卡、多 API 档案、PWA 修复、i18n 决策）保持待办。
4. `public/sw.js` 的 `CACHE_NAME` 版本号 +1（CLAUDE.md 的约定，几轮改动都没 bump 过）。
5. CLAUDE.md 若有新的「唯一来源」约定需补充（如 lorebook-trigger/staged-simulator 的归属）一并更新。
6. 合并 `feat/s3-followups` → main（ff-only），向用户汇报并提醒推送由其决定。

## 历史遗留的已知问题（不在本批，勿顺手修）

- `normalizeVariables` 的 `as VariableBlueprint[]` 不严谨断言（有代码注释）
- 依赖大版本升级（eslint 10 / TS 7 / react-hooks 7）
- SPA 内导航仍会丢就地编辑的未保存修改（需 data router + useBlocker）
- 备份导出的内存峰值（超大库移动端，roadmap 已记）
