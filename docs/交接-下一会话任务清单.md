# 交接清单 — 新会话从这里继续

> 更新于 2026-07-27（第二批 S3 收尾）。本文档自包含：不需要读上一会话的对话记录即可执行。
> 总规划见 [功能评估与开发规划-2026-07.md](功能评估与开发规划-2026-07.md)；已完成的工作看 `git log main`（提交说明写得很详细，是最好的上下文来源）。

## 当前状态快照

- **分支**：`feat/s3-followups` 已合并回 `main`（ff-only），可直接在 main 上继续。
- **工作区**：干净。typecheck ✅ / **703 测试全过** ✅ / 0 lint 错误（仅 1 个刻意保留的 SettingsPage 警告）/ 构建 ✅。
- **未推送**：整个 main 都没推过 origin，推送由用户决定，不要主动推。
- 上一批（本次会话）完成了旧清单的任务 1–6，详见下节。

## 上一批已完成（2026-07-27）

| 提交 | 内容 |
|---|---|
| `e697f67` | **MVU 变量引擎模拟器** `src/services/mvu-sim.ts` + 试聊接入。状态栏卡终于能验证数值绑定，不只是布局 |
| `c0546f9` | 工坊失败段一键补跑（checkpoint 加 `done` 相位） |
| `0dc64ba` | `description`/`personality` 保真、世界书名/描述保真、9 个条目 ext 字段改为导入值优先 |
| `cc3541c` | DIY / AI 自选阶段轴入口（此前管线已实现但无调用者） |
| `ebe0eb0` | i18n 文案键、SW `CACHE_NAME` v60、CLAUDE.md、路线图状态更新 |
| `92862a4` | 三轮对抗式复查确认的 15 条修复 |

**复查的重点产出**（三轮共确认 33 条并全部修复）：
- mvu-sim 安全：`__proto__` 原型污染（6 条向量实测可写全局 `Object.prototype`）、`SETVAR_ONLY_RE` 指数回溯（427 字节冻结页面）、深嵌套值触发渲染期 `RangeError` 掀翻整站、format 宏递归爆栈与二次回溯、`extractCommands` 对未闭合调用的二次复杂度。
- 导出保真：`_meta.entryIds` 存草稿 id 而导出把 id 重排成 1..N ⇒ 往返后「角色是否已同步」的判定结构性失效，角色描述被同时写进 `data.description` 和世界书条目（内容整份翻倍，往返不自愈）。现已改存映射后的导出 id。

## 工作方法约定（保持同一质量标准）

1. **每个逻辑提交独立可编译**：提交后逐个 checkout 跑 `npx tsc --noEmit` 验证。本批就靠它抓到一个提交划分错误（prop 的传入与声明分在了两个提交）。用后台任务跑，别在前台等（每个提交约 1.5 分钟）。
2. **每批改动落盘前跑对抗式复查**：用 Workflow 起 3-4 个视角的审查 agent，每条发现再起独立 agent 试图驳倒，只修被确认的。本批三轮分别确认 13/5/15 条（驳回 1/1/8）。这一步不能省——高危项全部来自它。
   - 复查 agent 会在仓库里留探针测试文件（`__probe_*.test.ts` 等），**复查结束后记得删**，否则会污染后续的 checkout sweep。
3. **并行 agent 严禁改 `src/i18n/translations.ts`**（并发读改写会互相丢编辑）：让它们报「键名+中文文案」清单，由主会话统一添加。注意：缺键时 `t()` 返回**空串**（不是键名）。
4. 提交说明用中文、讲清楚「为什么」与复查修了什么；`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
   - PowerShell 的 here-string 遇到消息里的引号会崩，长提交说明写到临时文件用 `git commit -F` 传。
5. TS strict 是开着的（TS6 默认，CLAUDE.md 有说明）；不要用 `any` 绕。

## 待执行任务

旧清单的 6 项已全部完成。下一批从路线图的剩余项里挑，按 ROI 排序建议：

### 优先：S2 残留（用户可感知的能力缺口）

- **S2-4 独立世界书 JSON 导入/导出**（ST world info 格式）。工坊产物目前只能经向导落到卡里，不能直接给已有卡用。
- **S2-7 MVU 专家模式内嵌状态栏配置** + 变量路径下拉/校验 + CDN 锁版本。专家模式现在要跳去别处配状态栏。
- **S2-6 库组织能力**：标签点击筛选、导入去重（覆盖/保留/跳过）、导出 PNG 用库封面、草稿箱聚合。

### 之后：S3 生态差异化（全部待办）

S3-1 三种输出（V2 PNG / V3 PNG 双 chunk / charx）、S3-2 Macros 2.0 校验、S3-3 ST 直连、S3-4 群聊卡、S3-5 多 API 档案、S3-6 直播间弹幕写端闭环、S3-7 PWA 修复（SW 清理改版本比对）、S3-8 i18n 决策（补 en 或砍掉 i18n 层）。

## 本批留下的已知取舍（不是 bug，别"顺手修"）

- **DIY 模式下「AI 剖析」按钮被禁用**：该按钮按模板的轴生成，DIY 无模板。点任一模板卡片即可切回模板模式。
- **工坊有失败段时不自动跳转向导**：导入文件的全文只在内存里（`importedFileText` 不落盘），跳走就再也补跑不了。改为留在工坊 + 提供「直接前往向导」按钮。
- **mvu-sim 刻意比上游更严**：`__proto__`/`constructor`/`prototype` 路径段一律拒绝、值嵌套深度上限 64、命令扫描字符预算 100 万。真实 MVU 用 lodash 自带前两项防护。
- **mvu-sim 的「已知差距」清单在模块头注释里**，改行为时必须同步更新它（UI 面板的说明文案与之对应）。

## 历史遗留的已知问题（不在待办，勿顺手修）

- `normalizeVariables` 的 `as VariableBlueprint[]` 不严谨断言（有代码注释）
- 依赖大版本升级（eslint 10 / TS 7 / react-hooks 7）
- SPA 内导航仍会丢就地编辑的未保存修改（需 data router + useBlocker）
- 备份导出的内存峰值（超大库移动端，roadmap 已记）
